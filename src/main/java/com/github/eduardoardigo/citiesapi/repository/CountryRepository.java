package com.github.eduardoardigo.citiesapi.repository;

import com.git
import org.springframework.data.jpa.repository.JpaRepository;

public interface CountryRepository extends JpaRepository<Country, Long> {
    
}
